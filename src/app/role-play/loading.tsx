import { PageLoading } from "@/components/page-loading";

export default function RolePlayLoading() {
  return (
    <PageLoading
      title="Loading role-play room"
      subtitle="Preparing the event, role, and historical perspective..."
    />
  );
}
